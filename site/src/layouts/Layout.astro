---
interface Props {
  title?: string;
}

const { title } = Astro.props;

const today = new Date()
  .toLocaleDateString("en-US", {
    year: "numeric",
    month: "long",
    day: "numeric",
  })
  .replace(/(\d+)(?=(,\s\d+)$)/, (match) => {
    const num = parseInt(match);
    const suffix = ["th", "st", "nd", "rd"][
      num % 10 > 3 || (num % 100) - (num % 10) == 10 ? 0 : num % 10
    ];
    return num + suffix;
  });
---

<!doctype html>
<html lang="en" class="bg-paper font-serif h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🌱</text></svg>"
    />
    <title>{title ? `${title} - Jack's Garden` : "Jack's Garden"}</title>
  </head>
  <body class="flex flex-col items-center h-full justify-between">
    <div
      class="prose prose-xl prose-orange prose-p:mt-0 prose-p:mb-4 prose-h1:mb-10 prose-h2:mt-0 max-w-[60ch] w-full"
    >
      <header class="px-8 pt-8 flex leading-none">
        <h1>
          <a
            href="/"
            class="no-underline hover:underline decoration-2 text-shade-900"
            >Jack's Garden 🌱</a
          >
        </h1>
      </header>
      <slot />
    </div>
    <footer
      class="flex items-center gap-4 justify-center text-sm text-base-500 w-full py-4"
    >
      <span>Jack's Garden 🌱</span><span>Last built on {today}</span>
    </footer>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
